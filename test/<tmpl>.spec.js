import {
	chai,
	sinon,
	describe,
	it,
	beforeEach,
	afterEach,
	expect,
} from './test.helper';

// import something from '../src/something.js';

describe("<test suite>", () => {
	
	beforeEach(() => {
		// setup
	});
	
	it("can test things", () => {
		
		expect(true).to.be.truthy;
		
	});
	
	afterEach(() => {
		// teardown
	});
	
});
